// Centralized constants used across the app

// Legacy key where older versions stored finished matches
export const LEGACY_MATCHES_KEY = 'wtr_matches';

// Local storage key for pending matches (offline queue)
export const LOCAL_PENDING_MATCHES_KEY = 'wtr_pending_matches';
